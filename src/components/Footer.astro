---
import { t, type Locale } from '../lib/i18n';

interface Props {
  locale: Locale;
}

const { locale } = Astro.props;
---
<footer>
  {t(locale, 'footer.disclaimer')}
  <a href="/submit">{t('en', 'nav.submit')}</a>
  <slot />
</footer>
