---
import PlayView from '../../components/PlayView.astro';
import { playStaticPaths } from '../../lib/plays';

export function getStaticPaths() {
  return playStaticPaths('en');
}

const { play, plays } = Astro.props;
---
<PlayView locale="en" play={play} plays={plays} />
