---
import BlogPostView from '../../../components/BlogPostView.astro';
import { blogStaticPaths } from '../../../lib/blog';

export function getStaticPaths() {
  return blogStaticPaths('zh');
}

const { post } = Astro.props;
---
<BlogPostView locale="zh" post={post} />
