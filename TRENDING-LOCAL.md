# Trending + Newest (local)

Astro stays static on port 4321.

Scores need Pages Functions plus local D1.

Apply the local table with wrangler 3.114.15 and the file in migrations/.

Serve the built site with wrangler pages on the dist folder.

Open a discover card on that host, then inspect local plays-stats rows.

Newest dates (day only): published, then date, then sourceDate, then X snowflake, then file mtime.

D1 name plays-stats. Binding PLAYS_DB. Function files live under functions/api/.
